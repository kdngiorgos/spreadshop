services:
  spreadshop:
    build: .
    env_file: .env
    ports:
      - "8080:8501"
    volumes:
      - ./cache:/app/cache
      - ./reports:/app/reports
      - ./logs:/app/logs
    environment:
      - SPREADSHOP_HEADLESS=true
    restart: unless-stopped
